/*
NULL일 경우 default값을 설정하고 싶다면 IFNULL AS,
SQL 문법 순서
SELECT
FROM 
WHERE
GROUP BY
HAVING
ORDER BY
*/
SELECT PT_NAME, PT_NO, GEND_CD, AGE, IFNULL(TLNO,'NONE') AS TLNO
FROM PATIENT
WHERE GEND_CD = 'W' && AGE <= 12
ORDER BY AGE DESC, PT_NAME ASC;
